{% extends '::base.html.twig' %}

{% block body -%}
    <h1>{% trans %}Edycja jednostki{% endtrans %} "{{ entity.name }}"</h1>

    {{ form(edit_form) }}

    <br/>
    <a href="{{ path('unit') }}" class="btn btn-default">
        {% trans %}Powrót do listy{% endtrans %}
    </a>

    {{ form(delete_form) }}
    <div class="clearfix"></div>
{% endblock %}
